// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Ensure experimental.appDir is not enabled since we're using Pages Router
};

export default nextConfig;
